server:
  forward-headers-strategy: framework

spring:
  application:
    name: duckswell
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 25MB
